console.log("Prabhat");

// Get elements by class name
// const boxes = document.getElementsByClassName("box");
// console.log(boxes);
// boxes[0].style.backgroundColor = "red";

// Get element by ID
// document.getElementById("red").style.background = "red";

// Query single element
// document.querySelector(".box").style.background = "green";

// Query all elements and update style
// document.querySelectorAll(".box").forEach(e => {
//     e.style.backgroundColor = "green";
// });

// Get elements by tag name
// console.log(document.getElementsByTagName("div"));

/*
 * matches():
 *   Checks if an element matches a given CSS selector.
 *   Returns true if it matches, false otherwise.
 *
 * closest():
 *   Traverses up the DOM tree to find the first ancestor matching a selector.
 *   Returns the ancestor element or null if not found.
 *
 * contains():
 *   Checks if a node is a descendant of another node.
 *   Returns true if it is, false otherwise.
 */